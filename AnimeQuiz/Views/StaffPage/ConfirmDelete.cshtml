@model AnimeQuiz.Models.StaffDto

@{
    ViewData["Title"] = $"Delete {Model.StaffName}";
}

<div class="d-flex justify-content-between align-items-center">
    <h1>@ViewData["Title"]</h1>
    <a class="text-decoration-none" href="javascript:history.back()">
        <h2>
            Back
        </h2>
    </a>
</div>

<hr />

<h3>Are you sure you want to delete this?</h3>

<div class="mt-3">
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.StaffName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.StaffName)
        </dd>
    </dl>

    <form asp-controller="StaffPage" asp-action="Delete" asp-route-id="@Model.StaffId">
        <button type="submit" class="btn btn-danger btn-lg">Delete</button>
    </form>
</div>
