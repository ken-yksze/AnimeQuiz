@model AnimeQuiz.Models.AnimeDto

@{
    ViewData["Title"] = $"Delete {Model.AnimeName}";
}

<div class="d-flex justify-content-between align-items-center">
    <h1>@ViewData["Title"]</h1>
    <a class="text-decoration-none" href="javascript:history.back()">
        <h2>
            Back
        </h2>
    </a>
</div>

<hr />

<h3>Are you sure you want to delete this?</h3>

<div class="mt-3">
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.AnimeName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.AnimeName)
        </dd>
    </dl>

    <form asp-controller="AnimePage" asp-action="Delete" asp-route-id="@Model.AnimeId">
        <button type="submit" class="btn btn-danger btn-lg">Delete</button>
    </form>
</div>
